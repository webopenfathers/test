<view class="container">
    <view class="item" bindtap="handleSelect" data-index='{{index}}' wx:for="{{count}}" wx:key="index">
        <i-icon name="{{icon}}" size="{{size}}" color='{{index<=currentIndex || index<selected?selectedColor:defaultColor}}'>
        </i-icon>
    </view>
</view>